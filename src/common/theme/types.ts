export interface ThemeStyles {
  theme: {
    palette: {
      primaryColor: string;
      secondaryColor: string;
      white: string;
      black: string;
    };
    component: {
      backgroundColor: string;
    };
  };
}
